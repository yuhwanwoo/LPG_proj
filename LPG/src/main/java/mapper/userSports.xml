<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="game.LPG.userSports">
	<insert id="insert" parameterType="userSports" >
		insert into userSports
		values(userSports_seq.nextVal,#{userId},#{sportsPosition},#{sportsTend},#{sportsFoot})
	</insert>
</mapper>